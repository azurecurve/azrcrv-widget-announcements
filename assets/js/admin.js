( function () {
	'use strict';

	// No dynamic behaviour needed yet - the Settings tab is a handful of
	// independent checkboxes with no client-side interactivity. Kept as its
	// own file (loaded alongside admin-standard.js) for structural parity
	// with the rest of the azurecurve plugin family, and so future settings
	// have somewhere to go without restructuring enqueues.

} )();
